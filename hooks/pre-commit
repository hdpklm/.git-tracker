#!/bin/sh
echo "PRE-COMMIT: ========================================================"
REPO_PATH="$(pwd)"
SCRIPT_DIR="$(dirname "$0")/.."
echo "Running: cd $SCRIPT_DIR && python git_tracker.py $REPO_PATH"
cd "$SCRIPT_DIR" && python git_tracker.py "$REPO_PATH"